timezone: "Australia/Melbourne"
data_config: true

collections_config:
  events:
    schemas:
      default:
        path: schemas/event.md

_inputs:
  content:
    type: markdown
  icon_svg:
    type: textarea
