timezone: "Australia/Melbourne"
data_config: true

collections_config:
  updates:
    path: content/updates
    schemas:
      default:
        path: schemas/update.md
  events:
    path: content/events
    schemas:
      default:
        path: schemas/event.md
_inputs:
  summary:
    type: markdown
  content:
    type: markdown
  icon_svg:
    type: textarea
